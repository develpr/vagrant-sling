#A simple vagrant box for sling#

Note that if NFS isn't working (if `vagrant up` results in errors related to NFS) then you can uncomment the sync related config options and comment/delete the nfs options.